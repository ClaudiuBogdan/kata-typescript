/**
 * Finds all occurrences of a pattern string within a text string using the KMP Algorithm.
 *
 * @param {string} text - The text string in which to search for the pattern.
 * @param {string} pattern - The pattern string to search for.
 * @returns {number[]} An array of starting indices where the pattern occurs in the text. An empty array if the pattern is not found.
 *
 * @example
 * kmpSearch("ABABDABACDABABCABAB", "ABABCABAB");  // returns [10]
 * kmpSearch("hello world", "world");  // returns [6]
 */
export function kmpSearch(text: string, pattern: string): number[] {
    // Function implementation here
}
