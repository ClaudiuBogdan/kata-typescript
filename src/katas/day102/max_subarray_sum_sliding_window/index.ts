/**
 * Finds the maximum sum of any contiguous subarray of size k.
 *
 * @param {number[]} nums - An array of integers.
 * @param {number} k - The size of the subarray.
 * @returns {number} The maximum sum of any contiguous subarray of size k.
 *
 * @example
 * const nums = [2, 1, 5, 1, 3, 2];
 * const k = 3;
 * maxSumSubarrayOfSizeK(nums, k);  // returns 9
 */
export function maxSumSubarrayOfSizeK(nums: number[], k: number): number {
    // Function implementation here
}
